import styled from "styled-components";

const StyledBalance = styled.div`
    width: calc(100% - 74px);
    padding: 3px 0 12px;
    position: fixed;
    left: 37px;
    bottom: 143px;
    z-index: 1;
    background-color: #ffffff;
`;

export{
    StyledBalance
}